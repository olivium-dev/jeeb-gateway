{
  "openapi": "3.0.0",
  "info": {
    "title": "push-notification (placeholder)",
    "version": "0.0.0",
    "description": "PLACEHOLDER: regenerate against a running instance using scripts/regenerate-clients.sh. This file is committed only so the BFF skeleton builds and contracts/ has an inventory entry for push-notification."
  },
  "paths": {},
  "components": {},
  "x-status": "PLACEHOLDER: fetch via scripts/regenerate-clients.sh against a running instance"
}
