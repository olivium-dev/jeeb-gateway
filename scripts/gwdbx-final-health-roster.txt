# A9 FINAL /health/ready roster (post W5-11). One name per line, sorted. 17 names below.
# Contract: docs/runbooks/gwdbx-deletion-ledger.md §7. Consumed by scripts/gwdbx-zero-dsn-smoke.sh.
# W5-13 2026-08-16: GatewayHealthRoster.ExpectedReadyCount is 18, not 17 — this list omits
# bundler-service, which is registered only when its BaseUrl key is set. RECONCILED against a real
# read of MSI /health/ready on 2026-08-16: it served 19 names = these 17 + bundler-service (its key
# IS set live) + role-service. Owner decision O8 retired role-service the same day, so the live
# roster is these 17 + bundler-service = 18, matching ExpectedReadyCount. The list below stays at 17
# because bundler-service is config-conditional and a cold boot without its key serves 17.
admin-oidc-configuration
ban-service
cdn-service
contract-signing-service
delivery-service
form-builder-service
geolocation-service
jeeb-state-service
notification-service
offer-service
push-notification
realtime-comunication-service
settlement-service
user-management
voice-transcription
wallet-service
whisper
