# Allowlist for scripts/check-no-50-server.sh — the .50 ban gate.
#
# Format:  <tracked path>:<exact trimmed content of the offending line>
# Lines starting with '#' and blank lines are ignored.
#
# RULES
#  1. Every entry needs a dated reason and a named owner of the decision that
#     unblocks it. No bare entries.
#  2. The gate FAILS on a stale entry (one matching nothing), so an exception
#     cannot outlive its reason. Removing the .50 line and its allowlist entry
#     is one atomic change.
#  3. This file is the visible inventory of the ban's open exceptions. It should
#     only ever shrink.
#
# ---------------------------------------------------------------------------
# EXCEPTION 1 — deploy-to-jeeb.yml workflow_dispatch defaults (10 lines)
# Added 2026-07-26 · batch b02-20260726 · P0 · blocked on D2 (token scope)
#
# These are the workflow_dispatch input DEFAULTS for 10 services. They are the
# highest-value part of the purge: a deploy that accepts the defaults repoints
# notification (:10026) and push (:10040) — plus 8 more legs — at an unroutable
# host, which breaks the polling→push cutover and presents as a product bug.
#
# NOT fixed here because the available GitHub token lacks the `workflow` OAuth
# scope and the remote hard-refuses any push touching .github/workflows/*:
#   "refusing to allow an OAuth App to create or update workflow
#    .github/workflows/i18n-gate.yml without `workflow` scope"
# (verified empirically on a throwaway branch, 2026-07-26 — not assumed).
#
# UNBLOCK: the owner rewrites these 10 defaults and deletes these 10 allowlist
# entries in one commit.
#
# DEAD POINTER, 2026-08-16: this paragraph used to name
# docs/batches/b02-20260726/OWNER-APPLY-workflow-50-purge.patch as the prepared
# fix. That path has never been committed to this repo (docs/batches/ does not
# exist), so there is no patch to apply — the rewrite has to be authored. The
# clause about wiring the gate into .github/workflows/ci.yml is dropped too:
# workflows are off-limits and disabled fleet-wide (A27), so promising CI
# enforcement here would be promising something nobody can deliver.
# ---------------------------------------------------------------------------
.github/workflows/deploy-to-jeeb.yml:chat_base_url:               { description: 'Chat service base URL',            required: true, default: 'http://192.168.2.50:10028' }
.github/workflows/deploy-to-jeeb.yml:user_management_base_url:    { description: 'User-management service base URL',  required: true, default: 'http://192.168.2.50:10001' }
.github/workflows/deploy-to-jeeb.yml:wallet_base_url:             { description: 'Wallet service base URL',          required: true, default: 'http://192.168.2.50:10014' }
.github/workflows/deploy-to-jeeb.yml:notification_base_url:       { description: 'Notification service base URL',     required: true, default: 'http://192.168.2.50:10026' }
.github/workflows/deploy-to-jeeb.yml:push_notification_base_url:  { description: 'Push-notification service base URL',required: true, default: 'http://192.168.2.50:10040' }
.github/workflows/deploy-to-jeeb.yml:otp_base_url:                { description: 'OTP service base URL',             required: true, default: 'http://192.168.2.50:10037' }
.github/workflows/deploy-to-jeeb.yml:matching_base_url:           { description: 'Matching service base URL (native host)', required: true, default: 'http://192.168.2.50:10025' }
.github/workflows/deploy-to-jeeb.yml:delivery_base_url:           { description: 'Delivery service base URL (live Jeeb swarm publishes on :10055 — S07 H6c accept->delivery-create)', required: true, default: 'http://192.168.2.50:10055' }
.github/workflows/deploy-to-jeeb.yml:kyc_base_url:                { description: 'Services__Kyc__BaseUrl — owning kyc-service (live swarm host)',  required: true, default: 'http://192.168.2.50:10074' }
.github/workflows/deploy-to-jeeb.yml:cdn_base_url:                { description: 'Services__Cdn__BaseUrl — cdn-service signed-PUT broker (live swarm host)', required: true, default: 'http://192.168.2.50:10072' }
#
# ---------------------------------------------------------------------------
# EXCEPTION 2 — Services:UnifiedPayment:BaseUrl — RESOLVED / CLOSED 2026-07-27
#
# This exception no longer exists. It described the one genuinely load-bearing
# .50 value in the repo: the UPG BaseUrl that selected HttpPaymentRefundClient
# for real dispute refunds (money OUT), which could not simply be blanked because
# blanking it swapped in a client that REPORTED SUCCESS for a refund that never
# happened.
#
# The owner ruled on 2026-07-27: "jeeb is only cash on delivery", no
# unified_payment_gateway. The resolution was NOT to blank the URL:
#   * the refund path now fails LOUDLY (CashOnDeliveryNoRefundClient throws) —
#     a refund that cannot be honoured must never be reported as honoured;
#   * HttpPaymentRefundClient, the UPG settlement client + its typed transport,
#     the db-probe-unified-payment client and the readiness probe are DELETED;
#   * the COD settlement ledger, which records cash ALREADY collected in person,
#     is served in-process — it tells the truth, so it is permanent, not a stub.
#
# No allowlist line is needed because no Services:UnifiedPayment key survives in
# committed config OR in the compiled assembly (verified with a UTF-16-aware
# scan of the built DLL — an ASCII-only grep previously produced a FALSE
# absence here). The standing guard is
# tests/JeebGateway.IntegrationTests/Payments/NoPaymentGatewayDialGuardTests.cs.
#
# The decision used to be cited as docs/batches/b02-20260726/UPG-REMOVAL.md.
# DEAD POINTER, 2026-08-16: docs/batches/ has never existed in this repo. The
# surviving record of the ruling is the paragraph above, plus the
# _comment_payments_REMOVED note in src/JeebGateway/appsettings.json (which
# carries the same dead path — treat its text, not its link, as the record).
# ---------------------------------------------------------------------------
